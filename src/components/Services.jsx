import { content } from "../Content";
import { createElement } from "react";

const Services = () => {
  const { services } = content;
  return (
    <section id="services">
      <div className="md:container px-5 py-14">
        <h4 className="subtitle2" data-aos="fade-down">
          {services.subtitle}
        </h4>
        <br />
        <div className="flex gap-5 justify-between flex-wrap group">
          {services.service_content.map((content, i) => (
            <div
              key={i}
              data-aos="fade-up"
              data-aos-delay={i * 600}
              className="min-w-[14rem] duration-300 cursor-pointer border-2 border-slate-200 rounded-xl shadow-black shadow-2xl text-center bg-cardLinear p-6 flex-1 group-hover:blur-sm 
              hover:!blur-none"
            >
              <div className="flex text-5xl place-content-center"><span>{createElement(content.icon)}</span></div>
              <img src="" alt="..." className="mx-auto" />
              <h6 className="my-3">{content.title}</h6>
              <p className="leading-7">{content.para}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
};

export default Services;
